{
  "name": "orbit-lab",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "start": "node scripts/server.mjs",
    "test": "node --test tests/content.test.mjs && playwright test"
  },
  "devDependencies": { "@playwright/test": "1.51.1" },
  "engines": { "node": ">=20" }
}
